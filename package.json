{
  "name": "vue-provide-observable",
  "version": "1.0.1",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "coffee --print src/*.coffee | babel --presets env > lib/index.js",
    "pub": "npm run build && npm publish"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "babel-cli": "^6.26.0"
  }
}
